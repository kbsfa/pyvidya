import React from 'react';

// FIX: This component is deprecated and should not be used.
// Per coding guidelines, the API key must be sourced exclusively from
// the `process.env.API_KEY` environment variable. The application
// must not prompt the user for an API key.
const ApiKeyModal: React.FC = () => {
  return null; // Render nothing
};

export default ApiKeyModal;
